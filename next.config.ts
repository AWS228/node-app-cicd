import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export", // ✅ This line tells Next.js to use static export
  // You can add more options here if needed
};

export default nextConfig;
